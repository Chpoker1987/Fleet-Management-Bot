# 🚛 Fleet Management Bot & Dashboard

Система автоматизированного управления автопарком через интерфейс Telegram с интегрированным Web-дашбордом.

## 📋 Описание проекта

Проект представляет собой гибридное решение (Telegram Bot + Web App), предназначенное для оптимизации логистических процессов. 
**Цель:** Обеспечить оперативный доступ к статусу транспортных средств и водителей в режиме реального времени.

**Основные характеристики:**
*   🤖 **Telegram Bot:** Интерфейс для водителей и менеджеров.
*   📊 **Dashboard:** Веб-интерфейс внутри Telegram (Mini App) для визуализации статистики.
*   🚀 **Async High-Performance:** Полностью асинхронное ядро (Python 3.12+, Aiogram 3, FastAPI).
*   💾 **SQL Persistence:** Надежное хранение данных (SQLAlchemy).

## 🛠 Установка и запуск

### Предварительные требования
*   Python 3.10+
*   Git

### Инструкция

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/YOUR_USERNAME/fleet-management-bot.git
    cd fleet-management-bot
    ```

2.  **Создайте виртуальное окружение:**
    ```bash
    python -m venv venv
    # Windows:
    venv\Scripts\activate
    # Linux/Mac:
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    *(Создайте `requirements.txt`: `aiogram`, `fastapi`, `uvicorn`, `sqlalchemy`, `aiosqlite`, `python-dotenv`, `jinja2`)*

4.  **Настройка окружения:**
    Создайте файл `.env` в корне проекта:
    ```env
    BOT_TOKEN=ваш_токен_от_botfather
    ```

5.  **Запуск:**
    ```bash
    python main.py
    ```

## 📱 Как пользоваться

1.  Откройте бота в Telegram.
2.  Нажмите `/start`.
3.  Бот приветствует вас и предлагает открыть **Дашборд**.
4.  Нажмите кнопку "Отрыть Дашборд" — откроется веб-окно со списком машин и их статусами.

*Примечание для локальной разработки: Для работы WebApp локально потребуется `ngrok` или туннель, чтобы Telegram мог видеть ваш локальный порт 8000.*

## 📞 Контакты

*   **GitHub:** [github.com/your-profile](https://github.com/)
*   **Email:** your.email@example.com
*   **Developer:** Your Name

---
*Developed with ❤️ and Python.*
